<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Dialecte MariaDB (au lieu de MySQL) -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MariaDBDialect</property>

        <!-- Driver JDBC pour MySQL/MariaDB -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver</property>

        <!-- URL de connexion -->
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/hibernate?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=UTC
        </property>

        <!-- Nom d'utilisateur -->
        <property name="hibernate.connection.username">root</property>

        <!-- Mot de passe (vide si pas de mot de passe) -->
        <property name="hibernate.connection.password"></property>

        <!-- Pool de connexions -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- Afficher les requêtes SQL dans la console -->
        <property name="hibernate.show_sql">true</property>

        <!-- Formater les requêtes SQL -->
        <property name="hibernate.format_sql">true</property>

        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Désactiver le cache de second niveau -->
        <property name="hibernate.cache.use_second_level_cache">false</property>

        <!-- Désactiver le cache de requêtes -->
        <property name="hibernate.cache.use_query_cache">false</property>

        <!-- Mappings des entités -->
        <mapping class="com.tp.entities.Salle"/>
        <mapping class="com.tp.entities.Machine"/>
    </session-factory>
</hibernate-configuration>